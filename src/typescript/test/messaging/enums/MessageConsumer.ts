// import { parentPort } from 'worker_threads';
// import { AppointmentsUpdatingMessages } from '../../../concepts/communication/Messages';

// parentPort?.on('message', (message) => {
// 	console.log('[MessageConsumer][parentPort on message] message: ', message);
// 	console.log(
// 		'[MessageConsumer][parentPort on message] message is StartUpdates ? ',
// 		message === AppointmentsUpdatingMessages.StartUpdates
// 	);
// });
